server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:oracle:thin:@//192.168.11.5:1521/orcl}
    username: ${DB_USERNAME:cmx}
    password: ${DB_PASSWORD:cmx}
  flyway:
    enabled: ${SPRING_FLYWAY_ENABLED:false}

jwt:
  secret: ${JWT_SECRET:dev-secret-key-for-local-development-only-2025}

app:
  admin-password-initializer:
    enabled: false
